@using Sandbox;
@using Sandbox.UI;
@inherits Hud

<root>

	<EnergyBar Owner="@Owner"/>
	<Inventory Owner="@Owner"/>

</root>

@code
{

	protected override void OnUpdate()
	{
		base.OnUpdate();

		if(Owner == null) { return; }

	}

	protected override int BuildHash() 
	{
		return System.HashCode.Combine(1
		);
	}
}
